name: test CI

on:
  push:
    branches: [ "dev" ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      # - name: Checkout repository
      #   uses: actions/checkout@v3

      - name: test
        env:
          TEST: ${{ secrets.TEST }}
        run: echo "${TEST}"
